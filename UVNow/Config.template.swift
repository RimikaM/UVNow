/// Copy this file to Config.swift and fill in your keys.
/// Config.swift is git-ignored and must never be committed.
enum Config {

    /// Free key from https://www.openuv.io (50 requests/day on free tier).
    /// Leave empty to use Open-Meteo only.
    static let openUVApiKey = ""
}
